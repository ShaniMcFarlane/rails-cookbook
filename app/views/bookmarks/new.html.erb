<h1>Add a recipe to <%= @category.name  %></h1>

<%= simple_form_for [@category, @bookmark] do |f| %>
  <%= f.association :recipe %>
  <%= f.input :comment %>
  <%= f.submit "add recipe" %>
<% end %>

<%= link_to "Back to all categories",  category_path(@category) %>
